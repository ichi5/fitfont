FitFont determines the font size automatically from the length of the width and character.

# Sample Usage

<pre>
// Default font size (10-15)
$("div").fitFont();

// Specifying font size
$("div").fitFont({minFontSize:20, maxFontSize:30});
</pre>
